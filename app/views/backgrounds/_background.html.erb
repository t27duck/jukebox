<div id="<%= dom_id background %>" class="w-full sm:w-auto my-5 space-y-5">
  <div>
    <strong class="block font-medium mb-1">Image:</strong>
    <%= link_to background.image.filename, background.image, class: "text-gray-700 underline hover:no-underline" if background.image.attached? %>
  </div>
</div>
